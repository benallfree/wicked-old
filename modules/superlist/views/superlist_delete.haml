.panel
  .title
    =se("Really delete")
    +$edit_object->name . "?"
  .body
    =se("This operation cannot be undone.")
    %p
      =button_to('delete', command_url('delete', array('c'=>1, 'oid'=>$edit_object->id), $namespace))
      =button_to('cancel', command_url('list', null, $namespace))
