<?

$config = array(
  /*
  Always generate the HAML, even if the file hasn't changed. This is good for debugging mode,
  especially on grid servers where file date stamps take a while to propagate.
  */
  'always_generate'=>true,  
);